#!/bin/bash
# Fiora 命令行工具包装脚本
# 此脚本用于执行 fiora 的各种管理命令

# 获取脚本所在目录的绝对路径
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

# 切换到项目根目录
cd "$PROJECT_ROOT"

# 执行 fiora 命令
exec npx ts-node index.ts "$@"

